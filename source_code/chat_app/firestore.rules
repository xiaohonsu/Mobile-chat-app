rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Demo mode: allow all reads and writes (emulator only)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
